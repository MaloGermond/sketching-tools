---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const year = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer-inner">

    <p class="footer-brand">Sketching Tools</p>

    <div class="footer-cols">
      <div class="footer-col">
        <p class="col-title">Navigate</p>
        <a href={`${base}/`} class="col-link">Home</a>
        <a href={`${base}/tools/warmup`} class="col-link">Warmup</a>
        <a href={`${base}/tools/drafts`} class="col-link">Sketchbook</a>
        <a href={`${base}/settings`} class="col-link">Réglages</a>
      </div>
      <div class="footer-col">
        <p class="col-title">Design</p>
        <a href={`${base}/style`} class="col-link">Style</a>
        <a href={`${base}/components`} class="col-link">Components</a>
      </div>
      <div class="footer-col">
        <p class="col-title">Legal</p>
        <a href={`${base}/legal/terms-and-conditions`} class="col-link">Terms and conditions</a>
        <a href={`${base}/legal/privacy-policy`} class="col-link">Privacy Policy</a>
      </div>
    </div>

    <div class="footer-divider"></div>

    <div class="footer-bottom">
      <p class="footer-copy">© {year} Sketching Tools. All rights reserved.</p>
      <p class="footer-author"><span class="by">By</span> <a href="https://malogermond.com" class="author-link" target="_blank" rel="noopener">Malo Germond</a></p>
    </div>

  </div>
</footer>

<style>
  .footer {
    /* Fond volontairement toujours sombre, quel que soit le thème du site */
    background: var(--footer-background);
    width: 100%;
  }

  .footer-inner {
    max-width: 62rem;
    margin: 0 auto;
    padding: 3rem 1.5rem 2rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .footer-brand {
    font-size: var(--text-2xl);
    font-weight: 500;
    color: var(--text-light);
  }

  .footer-cols {
    display: flex;
    flex-wrap: wrap;
    row-gap: 1.75rem;
    column-gap: 3.5rem;
  }

  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .col-title {
    font-size: var(--text-md);
    font-weight: 500;
    color: var(--text-light);
    margin-bottom: 0.25rem;
  }

  .col-link {
    font-size: var(--text-sm);
    font-weight: 300;
    color: var(--text-light);
    text-decoration: none;
    opacity: 0.7;
    transition: opacity 150ms ease;
  }

  .col-link:hover {
    opacity: 1;
  }

  .footer-divider {
    height: 1px;
    background: var(--color-gray-800);
  }

  .footer-bottom {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: flex-start;
    row-gap: 0.5rem;
    column-gap: 1.5rem;
  }

  .footer-copy {
    font-size: var(--text-xs);
    font-weight: 300;
    color: var(--text-light-subdue);
  }

  .footer-author {
    font-size: var(--text-xs);
    font-weight: 300;
    color: var(--text-light);
  }

  .by {
    font-weight: 100;
    color: var(--text-light-subdue);
  }

  .author-link {
    color: var(--text-light);
    text-decoration: none;
    transition: opacity 150ms ease;
  }

  .author-link:hover {
    opacity: 0.7;
  }
</style>
